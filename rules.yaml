# rules.yaml — edit freely
min_liq_usd: 5000
min_holders: 50
max_age_min: 60        # token must be <= 60 minutes old
min_mcap_usd: 10000
risk:
  allow_freeze: false
  allow_mint: false
  allow_blacklist: false
  allow_renounce_only: true
